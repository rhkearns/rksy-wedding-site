import React from 'react';
import "./directions.scss"

const Directions = () => {
	return (
		<div className="directions-page">
			<h1>Directions</h1>
			<h3>We will provide the directions closer to the date of the wedding</h3>
		</div>
	);
};

export default Directions;
